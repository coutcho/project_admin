// src/types/user.ts
// This type replaces the User type previously imported from @supabase/supabase-js
export interface User {
  id: string;
  email: string;
  // Add other user properties as needed by the frontend
}
